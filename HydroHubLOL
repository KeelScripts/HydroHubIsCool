-- LocalScript (put in StarterPlayer > StarterPlayerScripts)

local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local UserInputService = game:GetService("UserInputService")

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "TradeScriptGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- Main frame - even smaller rectangle
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 280, 0, 180)
frame.Position = UDim2.new(0.5, -140, 0.5, -90)
frame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
frame.BorderSizePixel = 0
frame.Active = true
frame.Parent = screenGui

-- Draggable title bar
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 35)
titleBar.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
titleBar.BorderSizePixel = 0
titleBar.Parent = frame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -90, 1, 0)
title.Position = UDim2.new(0, 10, 0, 0)
title.BackgroundTransparency = 1
title.Text = "Trade Script"
title.TextColor3 = Color3.fromRGB(180, 100, 255)
title.TextSize = 22
title.Font = Enum.Font.FredokaOne
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = titleBar

local credit = Instance.new("TextLabel")
credit.Size = UDim2.new(0, 80, 1, 0)
credit.Position = UDim2.new(1, -90, 0, 0)
credit.BackgroundTransparency = 1
credit.Text = "made by hydro"
credit.TextColor3 = Color3.fromRGB(140, 140, 160)
credit.TextSize = 11
credit.Font = Enum.Font.SourceSans
credit.TextTransparency = 0.5
credit.TextXAlignment = Enum.TextXAlignment.Right
credit.Parent = titleBar

-- Dragging logic
local dragging, dragInput, dragStart, startPos

local function update(input)
    local delta = input.Position - dragStart
    frame.Position = UDim2.new(
        startPos.X.Scale,
        startPos.X.Offset + delta.X,
        startPos.Y.Scale,
        startPos.Y.Offset + delta.Y
    )
end

titleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = frame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

titleBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        update(input)
    end
end)

-- 3 buttons without emojis, smaller size
local buttons = {
    {text = "FREEZE TRADE", color = Color3.fromRGB(120, 80, 220)},
    {text = "FORCE ACCEPT",  color = Color3.fromRGB(220, 60, 60)},
    {text = "ADD ALL",       color = Color3.fromRGB(60, 180, 90)}
}

for i, btnData in ipairs(buttons) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.88, 0, 0, 42)
    btn.Position = UDim2.new(0.06, 0, 0.23 + (i-1)*0.27, 0)
    btn.BackgroundColor3 = btnData.color
    btn.Text = btnData.text
    btn.TextColor3 = Color3.new(1,1,1)
    btn.TextSize = 20
    btn.Font = Enum.Font.FredokaOne
    btn.Parent = frame
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 6)
    btnCorner.Parent = btn
    
    btn.MouseButton1Click:Connect(function()
        -- nothing happens
    end)
end

-- Toggle with key "H"
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.H then
        screenGui.Enabled = not screenGui.Enabled
    end
end)

-- Start visible
screenGui.Enabled = true
